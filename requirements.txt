# VIOLA ETC — runtime dependencies (pinned to tested versions).
# Transitive dependencies (pandas, pyerfa, contourpy, botocore, ...) are
# resolved automatically by pip. The previous full `pip freeze` is recoverable
# from git history if an exact environment lock is ever needed.

streamlit==1.51.0
numpy==2.3.4
scipy==1.17.0          # molecules.py: scipy.signal.find_peaks
astropy==7.1.1         # sky_io.py: FITS I/O
matplotlib==3.10.7     # app.py: plotting
pillow==12.0.0         # app.py: page icon (PIL.Image)
boto3==1.42.49         # newera_io.py: only for s3:// PHOENIX-NewEra downloads
