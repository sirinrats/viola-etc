# Ignore all files by default
*

# Allow directories to be traversed
!*/

# Keep only PDF files in any subdirectory
!**/*.pdf

# But never ship intermediate build figures (regenerated by the LaTeX build)
technical_doc/figures/**

# Keep this file
!.gitignore
