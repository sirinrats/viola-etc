# Large data folders — distributed via S3 (or symlinked locally for dev)
stellar_models/*.csv

# Python / OS
__pycache__/
*.py[cod]
*$py.class
.DS_Store
.cache/
.ipynb_checkpoints/

# Streamlit secrets (NEVER commit)
.streamlit/secrets.toml

# Virtual environments
.venv/
venv/
env/

# Office lock files
~$*

# Editor
.vscode/
.idea/

# Technical documentation: documents/ has its own .gitignore that ships only
# PDFs (at any depth) and ignores LaTeX sources, build artifacts, bib/, figures.
